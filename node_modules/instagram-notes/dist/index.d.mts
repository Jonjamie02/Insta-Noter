import * as axios from 'axios';
import { Method, RawAxiosRequestHeaders } from 'axios';

declare class Note {
    sessionId: string;
    constructor(sessionId: string);
    private static formatCookie;
    static getSessionId(username: string, password: string): Promise<string>;
    privateRequest(method: Method, url: string, data?: any, headers?: RawAxiosRequestHeaders): Promise<axios.AxiosResponse<any, any>>;
    getNotes(): Promise<any>;
    createNote(text: string, audience?: 0 | 1): Promise<any>;
    deleteNote(id: string): Promise<any>;
    lastSeenUpdateNote(): Promise<any>;
}

export { Note as default };
